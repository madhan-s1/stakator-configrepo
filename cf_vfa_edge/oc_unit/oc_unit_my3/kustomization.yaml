apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: vfa-config-test
    namespace: ws-unit-domain
    files:
      - config/vfa.jar
    options:
      disableNameSuffixHash: true 
